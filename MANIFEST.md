# Repository Bundle Summary

## Repo: accessibility-audit-bot
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: actionlab-ablation-runner
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: ci-cost-optimizer
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: climate-risk-microtool
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: disaster-response-router
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: energy-usage-coach
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: fairness-monitor-lite
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: hospital-wait-forecast
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: incident-summarizer
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: inclusive-hiring-sim
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: misinformation-stance-mini
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: safe-rag-benchmark
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: secure-ml-ci
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: supply-chain-anomaly-watch
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

## Repo: water-quality-sentinel
- Secrets: `TAVILY_API_KEY`, `GEMINI_API_KEY`
- Local run: `python -m venv .venv && source .venv/bin/activate && pip install -e .[dev] && make pipeline`
- CI outputs: `artifacts/sources.json`, `artifacts/metrics.json`, `artifacts/figures/metric_plot.png`, `paper/paper.pdf`, `site/`
- Core files: README.md, pyproject.toml, config.yaml, scripts/, paper/, report/, docs/research_log.md, workflows

